package postVideosHandlerErrors

import "fmt"

type FileExistsError struct {
	FileName string
}

func (err FileExistsError) Error() string {
	return fmt.Sprintf("Cannont create new resource '%s' because it already exists", err.FileName)
}
